# 옛길 (Yetgil) — 포트폴리오/노션 작성용 종합 자료

> 이 문서는 노션 포트폴리오 페이지 작성(외부 작성자/도구에게 위임)을 위해 프로젝트의
> 모든 정보를 한 곳에 모은 자료다. 기능, 기술 스택, 화면 구성, 아키텍처, 개발 과정에서
> 겪은 문제와 해결 과정까지 전부 포함한다.

---

## 1. 한 줄 소개

**옛길**은 고향을 떠난 2030 세대가 예전에 살던 동네·학교 정보를 입력하면 그 시절의
모습을 돌아보고, AI가 추천하는 맞춤 여행 코스로 달라진 고향을 다시 여행하며, 같은
추억을 가진 사람들과 다시 연결될 수 있게 하는 **위치 기반 추억 여행 + 지역 커뮤니티
플랫폼**이다.

---

## 2. 기획 배경 / 문제의식 (서사)

- 고향을 떠나 타지에서 생활하는 인구가 늘어나면서, 예전에 살던 동네나 다녔던 학교에
  대한 추억을 나눌 곳이 마땅치 않다는 문제의식에서 출발했다.
- 기존 여행 앱은 관광지 정보를 나열하는 데 그치고, 사용자 개인의 상황(고향, 취향,
  동행자)을 반영한 맞춤형 경험을 제공하지 못한다는 한계가 있었다.
- 전국 단위로 운영되는 기존 커뮤니티 서비스와 달리, 실제 그 동네에 사는 사람들끼리
  정보와 추억을 나눌 수 있는 **동네 단위 커뮤니티**의 필요성도 함께 인식했다.
- 이 세 가지 문제의식(추억 연결, 개인화된 여행, 동네 커뮤니티)을 하나의 서비스로
  묶어 "옛길"을 기획했다.
- 사회적 가치 측면에서는, 고향을 떠난 2030 세대가 다시 고향을 방문·소비하도록
  유도함으로써 인구소멸 위기 지역에 관계인구를 유입시키는 효과도 기대할 수 있다.

---

## 3. 타겟 사용자

- 고향을 떠나 타지(주로 수도권)에서 생활하는 2030 세대
- 여행 코스를 짜는 데 시간을 쓰기 싫어하지만 개인화된 추천을 원하는 사용자
- 어릴 적 살던 동네나 학교 동창을 다시 찾고 싶은 사용자
- 자신이 사는 동네의 소식/맛집/정보를 이웃과 나누고 싶은 사용자

---

## 4. 전체 기능 목록 (상세)

### 4.1 AI 여행 코스 추천
- 지역명 또는 현재 위치(GPS 좌표)를 입력하면 AI(Claude)가 관광지를 골라 방문 순서,
  예상 소요 시간, 코스 분위기(산책/역사/미식 등), 감성 점수, 날씨 정보를 포함한
  코스를 자동으로 생성한다.
- 관광지 후보는 한국관광공사 TourAPI에서 가져오며, 사진이 확인된 장소 위주로
  후보를 구성한 뒤 Claude가 최종 방문 순서를 정한다.
- 좌표 기반으로 "내 주변" 코스도 생성할 수 있다.
- 코스 카드에는 대표 사진, 날씨, 예상 소요시간, 감성 점수 배지가 함께 표시된다.

### 4.2 AI 맛집(식당) 추천 및 코스 반영 — "식사 추가" 플로우
- 코스 상세 화면에서 "식사를 추가하시겠어요?"를 누르면 아침/점심/저녁 시간대,
  음식 카테고리(한식/중식/일식/양식/분식/카페 등, 다중 선택), 가격대, 연령대를
  선택할 수 있다.
- 백엔드는 TourAPI + 카카오 로컬 API로 코스 동선 주변 식당 후보를 모은 뒤(관측
  좌표 반경 검색, Redis로 1시간 캐싱), Claude에게 후보 목록과 "동선에서 얼마나
  벗어나는지"를 자연어 문구(예: "거의 벗어나지 않음")로 변환해 전달하여 최종 추천
  순위와 추천 이유(reason)를 자연스러운 한국어로 받는다.
- 여러 식사 시간대의 랭킹을 한 번의 Claude 호출로 배치 처리하여 API 호출 비용을
  절감했다.
- 식당 후보 카드는 큰 사진과 함께 표시되며, 사진이 없는 후보는 Google Places
  Photo API로 보강한다.
- "한 번에 채우기" 버튼으로 각 시간대 1순위 추천을 한 번에 반영할 수 있다.
- 실제 코스에 식당을 삽입할 때는 AI가 아니라 **결정론적 알고리즘**이 삽입 위치를
  계산한다 — 기존 두 정류지 사이 모든 위치에 식당을 넣어봤을 때 우회 거리
  `d(prev,new)+d(new,next)-d(prev,next)`가 최소가 되는 위치를 선택한다(할루시네이션
  없는 정확한 동선 유지).
- 식당을 추가한 뒤에는 "코스에 반영" → 반드시 "저장"해야 다음에 다시 봤을 때도
  남아 있다(저장하지 않으면 캐시성 데이터라 사라질 수 있음을 화면에서 안내).

### 4.3 나만의 코스 등록 & 공유
- 사용자가 직접 장소를 순서대로 담아 나만의 여행 코스를 만들 수 있다.
- 각 장소에 메모와 사진을 직접 등록할 수 있다(사진은 Google Cloud Storage에 저장).
- 완성한 코스는 다른 사용자에게 공유할 수 있고, 지도에서 전체 동선을 확인할 수 있다.
- 댓글 기능도 있다.

### 4.4 코스 저장(북마크) & 완주 체크
- AI 생성 코스와 직접 등록한 코스 모두 "저장"할 수 있다.
- AI 생성 코스는 저장 시점의 상태를 서버가 스냅샷으로 떠 두어, 캐시가 만료되거나
  식사가 추가된 이후에도 저장된 내용이 그대로 보존된다.
- 저장한 코스는 프로필의 "저장한 코스" 목록에서 다시 볼 수 있다.
- 코스 상세에서 "이 코스 완주했어요" 버튼으로 완주 기록을 남길 수 있다.

### 4.5 내 주변 / 둘러보기
- GPS 기반 지도에서 현재 위치 주변 장소를 검색하고 탐색할 수 있다.
- 관광지를 사진과 함께 탐색하는 "둘러보기" 화면이 별도로 있다.
- 카카오맵 로드뷰로 특정 장소의 실제 현재 모습을 확인할 수 있다(추억 속 장소와
  지금의 모습을 비교하는 용도).

### 4.6 지역 기반 커뮤니티
- 지역구 단위로 커뮤니티가 나뉘며, 자유/추억/주민/관광정보 4개 게시판이 있다.
- 게시판마다 카테고리(예: 취미/여가, 맛집/음식, 친구 등)를 다중 선택해 글을 쓸 수
  있고, 검색창 아래 카테고리 토글 필터로 게시글을 걸러볼 수 있다.
- 글쓰기는 Threads 스타일 UI — 프로필 사진 + 닉네임(볼드), 글 제목, 본문, 여러 장
  사진(스와이프), 조회수·좋아요·댓글·공유 버튼.
- 사진을 여러 장 첨부할 수 있고, 텍스트/사진을 섞은 블록 단위 글쓰기(블로그 스타일)
  를 지원한다.
- 댓글 작성/삭제, 대댓글(답글) 기능이 있다.
- 부적절한 게시글/댓글은 신고할 수 있고, 신고가 일정 수 누적되면 자동으로 숨김
  처리된다.
- 주민 게시판에서는 중고거래 글(가격, 거래상태: 판매중/예약중/거래완료)을 쓸 수
  있다.
- **타임캡슐**: 추억 게시판에 글을 쓸 때 미래의 "봉인 날짜"를 지정하면, 그 날짜가
  되기 전까지는 제목·내용·사진이 잠긴 채로만 보이다가 날짜가 되면 자동 공개된다.
- 우리 동네 설정(주소 검색 또는 GPS 역지오코딩으로 자동 추정), 여러 지역 커뮤니티
  가입 가능.
- 글쓰기 버튼은 원형 "+" 아이콘으로 단순화.

### 4.7 그 시절 이야기 / 고향 비교
- 등록한 고향/지역과 관련된 옛 뉴스와 이야기를 모아 보여주는 아카이브 기능
  (네이버 뉴스 검색 API 기반, 결과가 없을 때는 Claude가 지역 관련 이야기를 생성해
  큐레이션 데이터로 폴백).
- 고향이나 예전에 살던 동네를 등록하면 그 동네의 예전 모습과 지금 모습(로드뷰)을
  비교해볼 수 있다.

### 4.8 동창·이웃 찾기 (추억 매칭)
- 모교나 예전에 살았던 동네를 프로필에 등록(MemoryAttribute)하면, 같은 학교·동네
  출신인 다른 사용자를 찾아 연결(친구) 요청을 보낼 수 있다.
- "추억 프로필" 화면에서 상대방과의 연결 상태(none/pending/accepted)에 따라 다른
  액션을 보여준다.
- 연결이 수락되면 1:1 메시지를 주고받을 수 있다.
- 친구 찾기 기능 자체를 켜고 끌 수 있는 공개 설정이 있다.

### 4.9 AI 챗봇 "훈장님"
- Google Gemini(Vertex AI, `gemini-2.5-flash`) 기반 챗봇.
- 홈 화면 오른쪽 아래에 항상 떠 있는 주황색 챗봇 풍선(위에 "궁금한 사항을 훈장님께
  물어봐요" 말풍선 툴팁, x로 닫기 가능) — 풍선 자체는 상시 노출.
- 누르면 화면 아래에서 90%까지 올라오는 슬라이드 시트가 열린다(위로 끝까지 끌면
  전체화면, 아래로 끌면 닫힘 — DraggableScrollableController로 직접 구현).
- 처음 진입 시 캐릭터 일러스트(배경 이미지에 "훈장님" 타이틀/설명 문구가 함께
  그려져 있음)와 함께 "앱의 기능이 궁금하신가요?" / "옛길 앱 바탕으로 코스를
  짜드릴까요?" 두 버튼 중 하나를 선택해 대화를 시작한다.
- "앱 기능 안내" 모드: 앱의 모든 기능을 요약한 지식베이스를 시스템 프롬프트에
  실어 전달(RAG 방식)하여, 모르는 기능은 지어내지 않고 정직하게 모른다고 답하도록
  설계했다.
- "코스 짜드리기" 모드: 지역/취향/동행자를 대화로 물어보고 코스 아이디어를 제안한
  뒤, 실제 코스는 앱의 "추천 코스"/"코스 등록"에서 찾아보도록 안내한다.
- 캐릭터는 서당 훈장님 컨셉(따뜻하고 지혜로운 어른의 말투, 과하지 않은 존댓말).
- 답변 생성 중에는 로딩 스피너 대신 점 세 개가 파도치듯 움직이는 타이핑 인디케이터를
  보여준다.
- 대화 중 캐릭터 이미지는 상황별로 달라진다(첫 답변: 밝은 표정, 평상시: 기본 표정,
  오류 시: 미안한 표정).

### 4.10 프로필
- 통계 3칸: 등록한 코스 / 저장한 코스 / 등록한 게시글 (탭하면 각 목록 화면으로 이동).
- 정보 수정: 나이(연령대), 성별, 이름, 전화번호, 사는 곳.
- 모교 / 살았던 곳 등록·삭제(추억 매칭에 사용).
- 가입한 커뮤니티(지역) 목록 확인.
- 닉네임 변경, 프로필 사진 변경, 비밀번호 변경(카카오 로그인 계정은 비밀번호
  변경 불가).
- 알림 설정(댓글/좋아요 알림 켜고 끄기).
- 회원 탈퇴(옛길 계정 및 관련 데이터 삭제 + 카카오 연결 해제).

### 4.11 로그인 / 계정
- 아이디/비밀번호 자체 회원가입 — 휴대폰 본인인증(NCP SENS로 인증번호 발송) 필수.
- 카카오 소셜 로그인(네이티브 앱: 카카오톡 앱/카카오계정 로그인 SDK, 웹: 카카오
  인증 서버 리다이렉트 + authorization code 교환 방식) — 카카오 공식 로그인 버튼
  이미지를 그대로 사용.
- 네이버 로그인도 동일한 웹 리다이렉트 방식으로 백엔드/프론트 양쪽 다 구현
  완료했으나, 네이버 측 앱 등록·심사 절차가 번거로워 현재는 기능 플래그로 화면에서만
  숨겨두고 카카오만 노출 중(백엔드 엔드포인트/DB 컬럼은 이미 존재, 플래그만 켜면
  바로 노출 가능).
- 로그인 없이 "게스트로 입장"도 가능(일부 기능 제한).
- JWT 기반 access/refresh 토큰 발급, 자동 갱신.

### 4.12 알림
- 내 글에 댓글이나 좋아요가 달리면 알림을 받는다.
- 알림 설정에서 종류별로 끌 수 있다.

---

## 5. 기술 스택

### 5.1 프론트엔드 (Flutter)
- **프레임워크**: Flutter (Dart), 웹/Android 빌드 모두 지원(iOS는 미빌드)
- **상태관리**: flutter_riverpod ^3.3.2 (AsyncNotifier, FutureProvider 중심)
- **라우팅**: go_router ^17.3.0
- **네트워킹**: dio ^5.10.0
- **이미지**: cached_network_image ^3.4.1, image_picker ^1.2.0
- **지도/웹뷰**: webview_flutter ^4.14.0 (카카오맵 로드뷰 페이지 렌더링용)
- **카카오 SDK**: kakao_flutter_sdk_user, kakao_flutter_sdk_auth (^2.0.1)
- **보안 저장소**: flutter_secure_storage ^11.0.0 (토큰 저장)
- **위치**: geolocator ^14.0.2, permission_handler ^12.0.1
- **기타**: url_launcher(네이버 로그인 리다이렉트 등), http_parser

### 5.2 백엔드 (Python)
- **프레임워크**: FastAPI 0.121.2, uvicorn(standard) 0.40.0
- **ORM/DB 드라이버**: SQLAlchemy 2.0(비동기), asyncpg 0.31.0
- **마이그레이션**: Alembic 1.19.2
- **캐시**: redis 6.4.0 (Python 클라이언트, 관측/랭킹 결과 등 캐싱)
- **인증**: PyJWT 2.13.0, bcrypt 4.3.0
- **HTTP 클라이언트**: httpx 0.28.1 (모든 외부 API 호출에 사용)
- **AI SDK**: anthropic 1.4.0 (Claude API)
- **파일 업로드**: python-multipart
- **클라우드 스토리지**: google-cloud-storage 2.19.0
- **테스트**: pytest 8.4.2, pytest-asyncio 0.26.0

### 5.3 데이터베이스 / 인프라
- **DB**: PostgreSQL 16 (Google Cloud SQL, 인스턴스명 `yetgil-db`)
- **캐시**: Redis (Cloud Memorystore로 추정, VPC 커넥터 경유)
- **오브젝트 스토리지**: Google Cloud Storage (버킷 `yetgil-app-uploads`) — 프로필/
  게시글/커스텀 코스 사진을 영구 저장(Cloud Run 컨테이너 로컬 디스크는 인스턴스
  재시작 시 사라지는 휘발성 저장소라 전환).
- **컨테이너 배포**: Google Cloud Run (백엔드 서비스, 서비스명 `yetgil-backend`,
  리전 `asia-northeast3`), 컨테이너 시작 시 `alembic upgrade head` 자동 실행.
- **이미지 빌드**: Google Cloud Build → Artifact Registry(`yetgil-repo`).
- **비밀 관리**: Google Secret Manager (DB 접속정보, 각종 API 키).
- **네트워킹**: Cloud SQL Auth Proxy 연결(`--add-cloudsql-instances`), VPC 커넥터
  (`yetgil-connector`, Redis 등 프라이빗 IP 접근용).

### 5.4 AI / 외부 API
| 구분 | 서비스 | 용도 |
|---|---|---|
| AI | Anthropic Claude API (Claude Haiku 4.5 계열) | 코스 생성, 방문 순서 결정, 맛집 추천 랭킹·이유 문구 생성, 지역 이야기 생성 |
| AI | Google Gemini API (Vertex AI Express Mode, gemini-2.5-flash) | 챗봇 "훈장님" 대화 응답 생성 |
| 관광 데이터 | 한국관광공사 국문 관광정보 서비스(TourAPI, KorService2) | 관광지 명칭·좌표·설명 조회 |
| 관광 데이터 | 한국관광공사 관광사진 갤러리 서비스(PhotoGalleryService1) | 관광지 사진 조회 |
| 지도/장소 | Google Maps Platform API (Places, Geocoding 등) | 식당/장소 사진(Google Places Photo), 지도 표시 |
| 지도/장소 | 카카오 로컬 API(Kakao Local API) | 주소·좌표 변환, 주변 음식점 검색, 역지오코딩 |
| 로그인/지도 | 카카오 로그인, 카카오맵 API(SDK) | 소셜 로그인, 지도·로드뷰 표시 |
| 로그인 | 네이버 로그인 | 소셜 로그인(구현 완료, 현재 비노출) |
| 뉴스 | 네이버 뉴스 검색 API | "그 시절 이야기" 지역 뉴스 아카이브 |
| 알림 | 네이버클라우드플랫폼 SENS(Simple & Easy Notification Service) | 휴대폰 본인인증 문자 발송 |
| 날씨 | OpenWeatherMap API | 코스 지역 날씨 정보 |
| 저장소 | Google Cloud Storage | 사용자 업로드 사진 영구 저장 |

---

## 6. 시스템 아키텍처

### 6.1 백엔드 구조 (계층형)
```
app/
  api/routes/    ─ FastAPI 라우터 (auth, course, community, profile, chatbot, ...)
  models/        ─ Pydantic 요청/응답 스키마
  services/      ─ 비즈니스 로직 (CourseGeneratorService, MealPlannerService,
                    CommunityService, ChatbotService, NaverAuthService, ...)
  db/            ─ SQLAlchemy 모델(models.py), 세션(postgres.py), Redis(redis.py)
  core/          ─ 설정(config.py, .env 기반 Settings), 보안(JWT/bcrypt)
alembic/versions ─ DB 마이그레이션 이력
```
- 라우트는 서비스 계층에 위임하고, 서비스는 DB 세션과 외부 API 클라이언트를
  주입받는 구조(테스트에서 monkeypatch로 외부 호출을 쉽게 차단할 수 있게 설계).
- 코스 생성 로직은 두 갈래로 존재: `CourseGeneratorService`(사진 검증된 후보에서
  LLM이 방문지 선택, 지역/좌표 기반 추천용)와 `CoursePlanner`(규칙 기반 후보 스코어링
  + Claude "다듬기" 패스, `/api/course/generate` 전용) — 서로 다른 진입점에서
  발전한 별개 파이프라인.

### 6.2 프론트엔드 구조 (feature-first)
```
lib/
  core/          ─ 상수, 라우터(go_router), 테마(색상/타이포/그림자), 유틸
  data/          ─ 모델(models), 레포지토리(repositories, API 호출 담당)
  features/      ─ 화면 단위 모듈(auth, course, community, profile, chatbot,
                    custom_course, friend_finder, notifications, memory, ...)
                    각 feature는 data/(provider) + presentation/(화면·위젯)으로 분리
  shared/widgets ─ 여러 화면이 공유하는 위젯(AppNetworkImage, AppCard, PhotoFallback 등)
```
- Riverpod Provider 계층: Repository → Provider(Future/AsyncNotifier) → Widget이
  `ref.watch`로 구독하는 단방향 데이터 흐름.
- 이미지 URL 해석은 `resolveImageUrl`/`resolveStoredImageUrl`(core/utils/image_proxy.dart)
  한 곳으로 통일 — 백엔드가 상대경로(로컬 디스크) 또는 절대 URL(GCS)을 섞어 줘도
  프론트가 항상 올바르게 처리하도록 설계(초기엔 각 모델이 따로 처리해 버그가 있었음,
  6장 참고).

### 6.3 배포 파이프라인
- 백엔드: `gcloud builds submit`(Cloud Build) → Artifact Registry 이미지 → `gcloud
  run deploy`(Cloud Run). 배포마다 Alembic 마이그레이션이 컨테이너 시작 시 자동 적용.
- 프론트: `flutter build apk --release --split-per-abi`로 ABI별 릴리즈 APK 생성,
  원스토어(OneStore)에 업로드 후 원스토어 자체 앱 서명(서명키/업로드키 분리 관리)을
  거쳐 배포. 웹 버전은 `flutter run -d chrome`으로 로컬 검증(정식 도메인 배포는
  아직 진행 전).

---

## 7. 화면 구성 (라우트 전체 목록)

| 라우트 | 화면 |
|---|---|
| `/` | 홈(고향 검색, 하이라이트 배너, 바로가기, 공지, 오늘의 추억, 맛집 카테고리, 커뮤니티 미리보기, 카카오 맛집 카드) |
| `/compare/:locationId` | 고향 비교(그 시절 vs 지금) |
| `/archive/:locationId` | 그 시절 이야기(지역 뉴스 아카이브) |
| `/roadview/:locationId` | 로드뷰(웹뷰) |
| `/course`, `/course/:courseId` | 추천 코스 목록/상세 |
| `/explore` | 관광지 둘러보기 |
| `/nearby-map` | 내 주변 지도 검색 |
| `/restaurants/:category` | 카테고리별 맛집 목록 |
| `/kakao-restaurants` | 카카오맵 맛집 추천 |
| `/custom-courses`, `/custom-courses/create`, `/custom-courses/:courseId`, `/custom-courses/:courseId/edit`, `/custom-courses/:courseId/map` | 나만의 코스 등록/조회/수정/지도 |
| `/my-courses`, `/my-custom-courses`, `/saved-courses` | 등록한 코스 / 저장한 코스 목록 |
| `/community`, `/community/:region/:boardId`, `/community/write`, `/community-map/:region/:boardId` | 커뮤니티 게시판 목록/상세/글쓰기/지도 |
| `/post/:postId` | 게시글 상세(댓글 포함) |
| `/neighbors/:region`, `/neighbors/:region/:authorId`, `/my-posts/:authorId` | 동네 이웃, 특정 작성자 글 모음 |
| `/school-community/:schoolRegion`, `/classmates/:schoolRegion` | 학교 커뮤니티, 동창 목록 |
| `/memory-profile/:userId`, `/memory-timeline/:region` | 추억 프로필, 추억 타임라인 |
| `/friends/find`, `/friends/requests`, `/friends/connections`, `/friends/chat/:connectionId`, `/friends/my-attributes` | 친구 찾기, 요청, 연결 목록, 채팅, 내 추억 속성 관리 |
| `/my-memories` | 사진으로 남긴 추억 목록 |
| `/notifications`, `/blocked-users` | 알림, 차단한 사용자 |
| `/profile`, `/profile/info`, `/edit-profile` | 프로필, 정보 수정 |
| `/login`, `/signup` | 로그인, 회원가입 |
| `/about` | 서비스 소개 |

(챗봇은 별도 라우트가 아니라 홈 화면 위 슬라이드 시트로 열림)

---

## 8. 디자인 시스템 / UI

- **톤앤매너**: 웜 아이보리·베이지 배경 + 브라운 계열 강조색. 기존에 카테고리마다
  다른 파스텔 색(하늘/복숭아/민트 등)을 썼으나, 톤이 튀어 전체 통일감이 떨어진다는
  피드백을 반영해 앱 전체 파스텔을 베이지 톤 하나로 통일했다.
- **색상 토큰 예시**:
  - 배경(Paper) `#F2EFEA`, 표면(Surface) `#FFFDFC`, 입력창 배경(FieldBg) `#F6F3EE`
  - 본문 텍스트(Ink) `#29241F`, 보조 텍스트 `#91877D`, 옅은 텍스트 `#AAA198`
  - 브랜드 브라운: Primary `#755039`, Deep `#583B2A`, Interactive `#916247`,
    CTA `#AE704B`, Border `#A17C63`, Tint `#EFE4D9`
  - 챗봇 전용 포인트 컬러(의도적 예외): 주황 `#EF7B34`
- **타이포그래피**: 자체 AppTypography 스케일(title/headline/subhead/body/footnote/
  caption)로 통일.
- **카드/코너 라운딩**: 카드 12, 버튼 12, 필 형태(칩) 18, 작은 태그 6 — 예전엔
  18~22로 더 둥글었으나 "템플릿 느낌"이 난다는 이유로 축소.
- **홈 화면**: 상단 배너(닉네임/알림/프로필), 검색창+카테고리 토글, 하이라이트
  캐러셀, 4x2 바로가기 그리드(항목 사이 연한 구분선), 공지 배너, 오늘의 추억 카드,
  맛집 카테고리 캐러셀, 커뮤니티 미리보기, 카카오 맛집 카드.
- **커뮤니티 게시글 카드**: Threads 스타일 — 프로필 사진(원형) + 박스형 닉네임(볼드)
  헤더, 제목, 본문 미리보기, 여러 장 사진(스와이프), 조회수/좋아요/댓글/공유 아이콘 행.
- **로그인 화면**: 아이디/비밀번호 입력 → 로그인 버튼 → "또는" 구분선 → 네이버/카카오
  공식 로그인 버튼 이미지(원본 해상도 대비 과도하게 확대되지 않도록 최대 너비를
  제한해 화질 저하 방지).
- **챗봇 시트**: 반투명 없이 불투명 시트, 상단 드래그 핸들 바 + "AI 챗봇" 타이틀
  (볼드), 인트로 화면은 캐릭터 일러스트(고정 높이 배너, 텍스트/버튼과 겹치지 않게
  레이아웃 분리) + 두 개의 모드 선택 버튼, 대화 화면은 좌(캐릭터 아바타+베이지
  말풍선)/우(주황 말풍선) 채팅 버블.
- **반응형**: 홈 등 주요 화면은 `ConstrainedBox(maxWidth: ...)`로 넓은 화면(웹)에서도
  과도하게 늘어나지 않게 중앙 정렬.

---

## 9. 개발 과정에서 겪은 기술적 문제와 해결 (트러블슈팅 하이라이트)

포트폴리오 서사에 쓸 만한, 실제로 겪고 해결한 문제들이다.

1. **Cloud Run 휘발성 디스크 문제**: 초기에는 업로드 사진을 컨테이너 로컬 디스크에
   저장했는데, Cloud Run 인스턴스가 여러 개로 스케일되거나 재시작되면 디스크가
   초기화되어 사진이 갑자기 사라지는 현상이 있었다. Google Cloud Storage로 저장소를
   이전해 해결했다(설정값이 비어있으면 로컬 디스크, 채워져 있으면 GCS를 쓰도록
   분기해 로컬 개발 편의성도 유지).
2. **URL 이중 접두어 버그**: GCS로 전환한 뒤에도 커뮤니티 게시글 사진이 계속 기본
   이미지로만 보이는 버그가 있었다. 원인은 백엔드가 이미 완성된 GCS 절대 URL을
   내려주는데, 프론트엔드의 여러 모델(fromJson)이 관성적으로 그 앞에 API 서버
   주소를 또 붙여서 `https://백엔드주소https://storage.googleapis.com/...` 같은
   깨진 문자열을 만들고 있었던 것. 절대 URL인지 판별해 그대로 통과시키는 공용 함수
   (`resolveStoredImageUrl`)로 통일해 6개 이상의 모델에서 동일하게 재발하던 문제를
   구조적으로 제거했다.
3. **CORS 오진단 에피소드**: 위 버그를 고치는 과정에서 처음엔 "GCS 버킷에 CORS
   설정이 없어서 웹에서만 안 보이는 것"으로 오판하고 CORS를 먼저 열었다. 이후 친구가
   네이티브 앱(APK)에서도 안 보인다고 제보해, CORS는 브라우저 전용 제약이라 네이티브
   앱과 무관하다는 점에서 진짜 원인(URL 이중 접두어)을 다시 찾아낸 과정 자체가
   좋은 디버깅 사례.
4. **Gemini API 엔드포인트 함정**: 챗봇용으로 발급받은 Gemini API 키가 일반 AI
   Studio 키가 아니라 **Vertex AI Express Mode** 키였다(조직 정책상 다른 API로
   권한을 넓힐 수 없는 제약형 키). 표준 Generative Language API 엔드포인트
   (`generativelanguage.googleapis.com`)로는 403이 났고, Vertex AI 전용 엔드포인트
   + `x-goog-api-key` 헤더 방식으로 바꿔 해결했다.
5. **챗봇 답변이 중간에 끊기는 문제**: `gemini-2.5-flash`가 기본으로 켜져 있는
   "확장 사고(thinking)" 기능이 응답 토큰 예산을 답변과 나눠 쓰면서, 낮게 잡아둔
   maxOutputTokens 안에서 실제 눈에 보이는 답변 분량이 부족해져 끊기는 현상이
   있었다. `thinkingConfig.thinkingBudget: 0`으로 비활성화하고 예산을 늘려 해결.
6. **Flutter 드래그 가능 시트의 제스처 충돌**: 챗봇을 "아래에서 슬라이드로 올라오고
   위로 끌면 전체화면"으로 만들 때, `showModalBottomSheet`의 기본 "아래로 끌면
   닫기" 제스처와 `DraggableScrollableSheet`의 자체 리사이즈 제스처가 서로 같은
   세로 드래그를 두고 경쟁해 위로 끄는 동작만 죽는 문제가 있었다. 기본 드래그를
   끄고 `DraggableScrollableController`로 상단 손잡이의 드래그를 직접 제어하는
   방식으로 바꿔 양방향 모두 안정적으로 동작하게 만들었다.
7. **웹에서 마우스 드래그가 아예 안 먹는 문제**: Flutter 웹은 기본적으로 마우스를
   드래그 가능한 포인터로 인정하지 않는다(터치/스타일러스만 인정). 커스텀
   `ScrollBehavior`로 `PointerDeviceKind.mouse`를 드래그 허용 목록에 추가해 해결.
8. **로그인 계정 전환 시 이전 계정 데이터가 남는 버그**: 프로필/알림/저장한 코스 등
   "로그인한 사람 전용" Riverpod provider들이 로그인/로그아웃 시 캐시를 지우지
   않아서, A 계정으로 본 화면이 B 계정으로 다시 로그인해도 그대로 남아있는 문제가
   있었다. 인증 상태가 바뀔 때마다 사용자 전용 provider 11개를 한 번에 무효화하는
   중앙 함수로 재발을 구조적으로 방지했다.
9. **네이버 로그인의 실제 배포 제약**: 네이버 로그인은 카카오와 달리 별도의 앱
   심사(캡처 제출 등)가 필요해, 백엔드/프론트 구현은 끝냈지만 노출은 기능 플래그로
   잠가두고 카카오만 우선 노출하는 방식으로 실용적인 절충을 했다.
10. **원스토어 앱 서명과 카카오 로그인**: 원스토어가 자체 "앱 서명키"로 배포 APK를
    재서명하는 서비스를 제공하는데, 개발 중 써온 "업로드키"와 실제 배포되는
    "서명키"가 달라 카카오 로그인이 실제 배포본에서만 실패할 뻔한 문제를 사전에
    발견해, 두 키의 SHA-1 지문을 모두 카카오 개발자 콘솔의 키 해시로 등록해 예방했다.

---

## 10. 차별성

1. **추억 연결**: 단순 여행 정보 제공이 아니라, 고향·과거 거주 지역을 현재와
   연결해 향수를 자극하는 콘텐츠(그 시절 이야기, 로드뷰 비교)를 제공한다.
2. **동창 찾기**: 모교·거주 이력 기반으로 같은 학교·동네 출신 사용자를 찾아 실제
   인간관계 재형성을 지원한다.
3. **코스 공유 및 커뮤니티**: 직접 다녀온 코스를 기록·공유하고, 동네 단위 커뮤니티로
   실질적인 지역 정보 교류가 가능하다.
4. **AI를 여행의 전 과정에 결합**: 코스 설계 → 식사 추천 → 챗봇 상담까지 AI가
   개입하되, 실제 동선 삽입은 결정론적 알고리즘으로 처리해 "AI가 지어낸 이상한
   동선"이 되지 않도록 설계했다.

---

## 11. 발전 계획

1. 네이버 등 소셜 로그인 수단 확대(이미 구현, 심사 후 노출 예정)
2. 사용자 리뷰·평점 기반 추천 정확도 고도화
3. 지역 소상공인 제휴(맛집 인증·할인 혜택 연동)
4. 실시간 소통 기능 추가로 커뮤니티 활성화
5. 외국인 관광객 대상 다국어 지원

---

## 12. 사용 데이터 출처 정리

**한국관광공사**
- 국문 관광정보 서비스(TourAPI, KorService2) — 관광지 정보 조회
- 관광사진 갤러리 서비스(PhotoGalleryService1) — 관광지 사진 조회

**외부(한국관광공사 제외)**
- Anthropic Claude API — 코스 생성, 맛집 랭킹, 챗봇 일부 로직
- Google Gemini API(Vertex AI) — 챗봇 대화 생성
- Google Maps Platform API — 지도, 장소 사진
- 카카오 로컬 API — 좌표 변환, 주변 검색
- 카카오 로그인 / 카카오맵 API(SDK)
- 네이버 뉴스 검색 API — 지역 뉴스 아카이브
- 네이버클라우드플랫폼 SENS — 휴대폰 인증 문자
- OpenWeatherMap API — 날씨
- Google Cloud Storage — 사진 저장

---

## 13. 팀 / 역할

- 백엔드·프론트엔드·인프라·AI 파이프라인 개발을 팀원들과 함께 진행했으며, 이 문서를
  준비한 세션에서는 주로 신규 기능 추가(식사 추천, 챗봇, 소셜 로그인)와 버그 수정,
  배포 작업을 다뤘다. (팀원별 정확한 역할 분담은 별도로 채워 넣을 것)

---

## 14. 참고: 이 문서 작성 시점의 배포 상태

- 백엔드: Google Cloud Run에 배포되어 실제 동작 중 (`yetgil-backend`,
  `asia-northeast3`).
- 프론트엔드: Android APK 빌드 완료, 원스토어 앱 등록 및 심사 통과. 웹 버전은
  로컬 검증만 진행했고 별도 도메인 배포는 아직 없음.
- 테스트 계정: 아이디/비밀번호 로그인 지원, 카카오 로그인도 지원.
